# Normalise line endings in the repository and check out LF everywhere.
# Without this, a Windows clone gets CRLF and `npm run format:check` fails on a
# tree that is byte-correct in git.
* text=auto eol=lf

# Generated by `npm run bench`; the generator owns its formatting.
bench/RESULTS.md -diff
